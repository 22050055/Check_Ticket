# ══════════════════════════════════════════════════════════════
# Tourism Access Control — Backend requirements
# Python >= 3.11
# ══════════════════════════════════════════════════════════════

# ── Web Framework ─────────────────────────────────────────────
fastapi>=0.111.0
uvicorn[standard]>=0.30.0        # standard = websockets + httptools
pydantic>=2.7.0
pydantic-settings>=2.3.0         # đọc .env vào Settings class
python-multipart>=0.0.9          # multipart/form-data upload
starlette>=0.37.0                # BaseHTTPMiddleware (AuditMiddleware)
email-validator>=2.0.0           # dành cho validate EmailStr trong pydantic

# ── MongoDB Atlas ─────────────────────────────────────────────
motor>=3.4.0                     # async MongoDB driver (wrap pymongo)
pymongo[srv]>=4.7.0              # [srv] bắt buộc cho Atlas SRV URI (mongodb+srv://)

# ── Auth & Security ───────────────────────────────────────────
python-jose[cryptography]>=3.3.0 # JWT encode/decode RS256 + HS256
passlib[bcrypt]>=1.7.4           # password hashing
bcrypt==4.0.1                    # Giảm version bcrypt để không bị lỗi __about__ thiếu sót của passlib

# ── HTTP client (gọi AI Services) ─────────────────────────────
httpx>=0.27.0                    # async HTTP → /enroll /verify (port 8001)

# ── QR generation ─────────────────────────────────────────────
qrcode[pil]>=7.4.2               # tạo ảnh QR PNG (Pillow backend)
Pillow>=10.3.0                   # image processing cho qrcode

# ── Type hints ────────────────────────────────────────────────
typing-extensions>=4.11.0        # TypedDict, NotRequired trong models/__init__.py

# ── Utils ─────────────────────────────────────────────────────
python-dotenv>=1.0.0             # load .env khi chạy local (không cần trong Docker)
 